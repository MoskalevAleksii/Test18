// Проект le10

// le10.Res.SLe10Resource	"SLe10Resource.h"	// Доступ к ресурсам этой сборки

// le10.IInfoObject			"IInfoObject.h"		// ++Интерфейс с информацией об обьекте (Устарело)
// le10.TDotUserObj			"TDotUserObj.h"		// ++Объект пользователя в виде точки	(Устарело)
// le10.TEditUserObj		"TEditUserObj.h"	// ++Редактируемый объект пользователя	(Устарело)
// le10.TIconUserObj		"TIconUserObj.h"	// ++Обьект пользователя в виде иконки	(Устарело)

// le10.TRsMemMap			"TRsMemMap.h"		// Невизуальная карта GeoModule Robosoft
// le10.TRsMap2				"TRsMap2.h"			// Карта GeoModule Robosoft

// le10.MapObj.TMapObjIcon	"TMapObjIcon.h"		// Объект отображения в виде иконки с текстом


